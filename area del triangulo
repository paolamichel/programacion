// Autor:paola michel cuellar
// fecha:

#include "stdafx.h"
#include <iostream>
#include "conio.h"

using namespace std;
void main ()


{
   int a,b,c;
   cout<<"ingresar a:";
   cin>>a;
   cout<<"ingrese b:";
   cin>>b;
   cout<<"ingrese c:";
   cin>>c;
   if   (a>b)
	   if   (a>c)
		   cout<<a;
	   else
		   cout<<c;
       if   (b>c)
		   cout<<b;
	   else
		   cout<<c;
getche();
}
